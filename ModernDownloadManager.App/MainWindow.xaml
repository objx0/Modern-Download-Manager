<Window x:Class="ModernDownloadManager.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:ModernDownloadManager.App.ViewModels">
    <Grid x:Name="RootGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.Resources>
            <Style x:Key="ToolbarButton" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="CornerRadius" Value="2"/>
                <Setter Property="MinWidth" Value="84"/>
                <Setter Property="Height" Value="82"/>
                <Setter Property="Padding" Value="12,8"/>
                <Setter Property="UseSystemFocusVisuals" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid x:Name="ButtonSurface" Background="Transparent" CornerRadius="2">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters><Setter Target="ButtonSurface.Background" Value="{ThemeResource SubtleFillColorSecondaryBrush}"/></VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters><Setter Target="ButtonSurface.Background" Value="{ThemeResource SubtleFillColorTertiaryBrush}"/></VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="ButtonSurface.Background" Value="Transparent"/>
                                                <Setter Target="ButtonContent.Opacity" Value="0.38"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <ContentPresenter x:Name="ButtonContent" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}"
                                    Padding="{TemplateBinding Padding}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="TableText" TargetType="TextBlock">
                <Setter Property="Margin" Value="10,0"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                <Setter Property="FontSize" Value="13"/>
            </Style>
            <Style x:Key="TableCell" TargetType="Border">
                <Setter Property="BorderBrush" Value="{ThemeResource DividerStrokeColorDefaultBrush}"/>
                <Setter Property="BorderThickness" Value="0,0,1,0"/>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/><RowDefinition Height="28"/>
        </Grid.RowDefinitions>
        <Grid x:Name="AppTitleBar" Padding="14,0,144,0">
            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE896;" FontSize="16" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                <TextBlock Text="Modern Download Manager" FontSize="13" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>
        <MenuBar Grid.Row="1">
            <MenuBarItem Title="Tasks">
                <MenuFlyoutItem Text="Add URL…" Click="AddDownload_Click"/>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="Resume" Command="{Binding SelectedDownload.ResumeCommand}" IsEnabled="{Binding SelectedDownload.CanResume, FallbackValue=False, TargetNullValue=False}"/>
                <MenuFlyoutItem Text="Stop" Command="{Binding SelectedDownload.PauseCommand}" IsEnabled="{Binding SelectedDownload.CanPause, FallbackValue=False, TargetNullValue=False}"/>
                <MenuFlyoutItem Text="Stop all" Command="{Binding StopAllCommand}"/>
            </MenuBarItem>
            <MenuBarItem Title="File">
                <MenuFlyoutItem Text="Open file" Command="{Binding SelectedDownload.OpenFileCommand}" IsEnabled="{Binding SelectedDownload.CanOpenFile, FallbackValue=False, TargetNullValue=False}"/>
                <MenuFlyoutItem Text="Open folder" Command="{Binding SelectedDownload.OpenFolderCommand}" IsEnabled="{Binding SelectedDownload.CanOpenFolder, FallbackValue=False, TargetNullValue=False}"/>
                <MenuFlyoutSeparator/><MenuFlyoutItem Text="Exit" Click="Exit_Click"/>
            </MenuBarItem>
            <MenuBarItem Title="Downloads">
                <MenuFlyoutItem Text="Delete selected…" Command="{Binding SelectedDownload.RemoveCommand}" IsEnabled="{Binding HasSelection}"/>
                <MenuFlyoutItem Text="Clear completed from list…" Click="ClearCompleted_Click"/>
                <MenuFlyoutSeparator/><MenuFlyoutItem Text="Options" Click="Options_Click"/>
            </MenuBarItem>
            <MenuBarItem Title="View">
                <MenuFlyoutItem Text="All downloads" Click="AllDownloads_Click"/>
                <MenuFlyoutItem Text="Show / hide categories" Click="AppTitleBar_PaneToggleRequested"/>
            </MenuBarItem>
            <MenuBarItem Title="Help"><MenuFlyoutItem Text="About" Click="About_Click"/></MenuBarItem>
        </MenuBar>
        <Border Grid.Row="2" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,1,0,1" Background="{ThemeResource SubtleFillColorSecondaryBrush}">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" HorizontalScrollMode="Enabled" VerticalScrollMode="Disabled">
                <StackPanel Orientation="Horizontal" Padding="6,4" Spacing="2">
                    <Button Style="{StaticResource ToolbarButton}" Click="AddDownload_Click" AutomationProperties.Name="Add URL">
                        <StackPanel Spacing="10"><FontIcon Glyph="&#xE710;" FontSize="28" Foreground="#56A998"/><TextBlock Text="Add URL" HorizontalAlignment="Center" FontSize="13"/></StackPanel>
                    </Button>
                    <Button Style="{StaticResource ToolbarButton}" Command="{Binding SelectedDownload.ResumeCommand}" IsEnabled="{Binding SelectedDownload.CanResume, FallbackValue=False, TargetNullValue=False}" AutomationProperties.Name="Resume">
                        <StackPanel Spacing="10"><FontIcon Glyph="&#xE768;" FontSize="28" Foreground="#579BC6"/><TextBlock Text="Resume" HorizontalAlignment="Center" FontSize="13"/></StackPanel>
                    </Button>
                    <Button Style="{StaticResource ToolbarButton}" Command="{Binding SelectedDownload.PauseCommand}" IsEnabled="{Binding SelectedDownload.CanPause, FallbackValue=False, TargetNullValue=False}" AutomationProperties.Name="Stop">
                        <StackPanel Spacing="10"><FontIcon Glyph="&#xE71A;" FontSize="28" Foreground="#D49A69"/><TextBlock Text="Stop" HorizontalAlignment="Center" FontSize="13"/></StackPanel>
                    </Button>
                    <Button Style="{StaticResource ToolbarButton}" Command="{Binding StopAllCommand}" AutomationProperties.Name="Stop all">
                        <StackPanel Spacing="10"><Grid Width="30" Height="28" HorizontalAlignment="Center">
                            <Path Data="M 9,2 L 27,2 L 27,20" Stroke="#C88970" StrokeThickness="2"/>
                            <Rectangle Width="20" Height="20" RadiusX="2" RadiusY="2" Stroke="#C88970" StrokeThickness="2" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
                        </Grid><TextBlock Text="Stop all" HorizontalAlignment="Center" FontSize="13"/></StackPanel>
                    </Button>
                    <Button Style="{StaticResource ToolbarButton}" Command="{Binding SelectedDownload.RemoveCommand}" IsEnabled="{Binding HasSelection}" AutomationProperties.Name="Delete">
                        <StackPanel Spacing="10"><FontIcon Glyph="&#xE74D;" FontSize="28" Foreground="#BA7C82"/><TextBlock Text="Delete" HorizontalAlignment="Center" FontSize="13"/></StackPanel>
                    </Button>
                    <Button Style="{StaticResource ToolbarButton}" Click="ClearCompleted_Click" AutomationProperties.Name="Clear completed">
                        <StackPanel Spacing="10"><FontIcon Glyph="&#xE73E;" FontSize="28" Foreground="#A77BBC"/><TextBlock Text="Clear completed" HorizontalAlignment="Center" FontSize="13"/></StackPanel>
                    </Button>
                    <Button Style="{StaticResource ToolbarButton}" Click="Options_Click" AutomationProperties.Name="Options">
                        <StackPanel Spacing="10"><FontIcon Glyph="&#xE713;" FontSize="28" Foreground="#9E83B8"/><TextBlock Text="Options" HorizontalAlignment="Center" FontSize="13"/></StackPanel>
                    </Button>
                    <Button Style="{StaticResource ToolbarButton}" Command="{Binding ResumeAllCommand}" AutomationProperties.Name="Resume all">
                        <StackPanel Spacing="10"><FontIcon Glyph="&#xE768;" FontSize="28" Foreground="#619BB2"/><TextBlock Text="Resume all" HorizontalAlignment="Center" FontSize="13"/></StackPanel>
                    </Button>
                </StackPanel>
            </ScrollViewer>
        </Border>
        <Grid Grid.Row="3" Padding="6,6,6,0" ColumnSpacing="6">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Border x:Name="CategoriesPane" Width="222" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="1">
                <Grid><Grid.RowDefinitions><RowDefinition Height="34"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <TextBlock Text="Categories" Margin="10,0" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <TreeView x:Name="CategoriesTree" Grid.Row="1" SelectionMode="Single" ItemInvoked="Categories_ItemInvoked">
                        <TreeView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="{Binding Content, Converter={StaticResource CategoryGlyph}}" FontSize="16" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                                    <TextBlock Text="{Binding Content}" FontSize="13"/>
                                </StackPanel>
                            </DataTemplate>
                        </TreeView.ItemTemplate>
                        <TreeView.RootNodes>
                            <TreeViewNode Content="All downloads" IsExpanded="True">
                                <TreeViewNode.Children>
                                    <TreeViewNode Content="Compressed"/>
                                    <TreeViewNode Content="Documents"/>
                                    <TreeViewNode Content="Music"/>
                                    <TreeViewNode Content="Programs"/>
                                    <TreeViewNode Content="Video"/>
                                    <TreeViewNode Content="Images"/>
                                </TreeViewNode.Children>
                            </TreeViewNode>
                            <TreeViewNode Content="Unfinished"/>
                            <TreeViewNode Content="Finished"/>
                            <TreeViewNode Content="Queued"/>
                        </TreeView.RootNodes>
                    </TreeView>
                </Grid>
            </Border>
            <Grid Grid.Column="1">
                <Grid Visibility="{Binding IsDownloadsView, Converter={StaticResource BoolToVisibility}}">
                    <Grid.RowDefinitions><RowDefinition Height="38"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <Grid Margin="0,0,0,5">
                        <TextBlock Text="{Binding HeaderText}" Margin="10,0" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    </Grid>
                    <Border Grid.Row="1" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="1">
                        <ScrollViewer HorizontalScrollBarVisibility="Auto" HorizontalScrollMode="Enabled" VerticalScrollBarVisibility="Disabled" VerticalScrollMode="Disabled">
                            <Grid MinWidth="1160">
                                <Grid.RowDefinitions><RowDefinition Height="34"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                                <Grid Background="{ThemeResource SubtleFillColorSecondaryBrush}" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
<Grid.ColumnDefinitions><ColumnDefinition Width="360"/><ColumnDefinition Width="180"/><ColumnDefinition Width="110"/><ColumnDefinition Width="110"/><ColumnDefinition Width="110"/><ColumnDefinition Width="190"/></Grid.ColumnDefinitions>
<Border Grid.Column="0" Style="{StaticResource TableCell}"><TextBlock Text="File name" Style="{StaticResource TableText}"/></Border>
<Border Grid.Column="1" Style="{StaticResource TableCell}"><TextBlock Text="Size" Style="{StaticResource TableText}"/></Border>
<Border Grid.Column="2" Style="{StaticResource TableCell}"><TextBlock Text="Status" Style="{StaticResource TableText}"/></Border>
<Border Grid.Column="3" Style="{StaticResource TableCell}"><TextBlock Text="Time left" Style="{StaticResource TableText}"/></Border>
<Border Grid.Column="4" Style="{StaticResource TableCell}"><TextBlock Text="Transfer rate" Style="{StaticResource TableText}"/></Border>
<Border Grid.Column="5" Style="{StaticResource TableCell}"><TextBlock Text="Date completed" Style="{StaticResource TableText}"/></Border>
                                </Grid>
                                <ListView x:Name="DownloadsTable" Grid.Row="1" ItemsSource="{Binding VisibleDownloads}" SelectedItem="{Binding SelectedDownload, Mode=TwoWay}" SelectionMode="Single" IsItemClickEnabled="False" DoubleTapped="DownloadsTable_DoubleTapped" Padding="0">
                                    <ListView.ItemContainerStyle><Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                        <Setter Property="Padding" Value="0"/>
                                        <Setter Property="Margin" Value="0"/>
                                        <Setter Property="MinHeight" Value="34"/>
                                        <Setter Property="CornerRadius" Value="0"/>
                                    </Style></ListView.ItemContainerStyle>
                                    <ListView.ItemTemplate><DataTemplate x:DataType="vm:DownloadItemViewModel">
                                        <Grid Height="34" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                                            <Grid.ContextFlyout><MenuFlyout>
                                                <MenuFlyoutItem Text="Resume" Command="{Binding ResumeCommand}" IsEnabled="{Binding CanResume}"/>
                                                <MenuFlyoutItem Text="Stop" Command="{Binding PauseCommand}" IsEnabled="{Binding CanPause}"/>
                                                <MenuFlyoutItem Text="Download details" Command="{Binding OpenDownloadWindowCommand}" IsEnabled="{Binding CanShowDownloadWindow}"/>
                                                <MenuFlyoutSeparator/>
                                                <MenuFlyoutItem Text="Open file" Command="{Binding OpenFileCommand}" IsEnabled="{Binding CanOpenFile}"/>
                                                <MenuFlyoutItem Text="Open folder" Command="{Binding OpenFolderCommand}" IsEnabled="{Binding CanOpenFolder}"/>
                                                <MenuFlyoutItem Text="Delete…" Command="{Binding RemoveCommand}"/>
                                            </MenuFlyout></Grid.ContextFlyout>
<Grid.ColumnDefinitions><ColumnDefinition Width="360"/><ColumnDefinition Width="180"/><ColumnDefinition Width="110"/><ColumnDefinition Width="110"/><ColumnDefinition Width="110"/><ColumnDefinition Width="190"/></Grid.ColumnDefinitions>
<Border Grid.Column="0" Style="{StaticResource TableCell}"><StackPanel Orientation="Horizontal" Spacing="8" Margin="10,0" VerticalAlignment="Center"><FontIcon Glyph="{Binding CategoryText, Converter={StaticResource CategoryGlyph}}" FontSize="15" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/><TextBlock Text="{Binding FileName}" Width="310" TextTrimming="CharacterEllipsis" FontSize="13" ToolTipService.ToolTip="{Binding FileName}"/></StackPanel></Border>
<Border Grid.Column="1" Style="{StaticResource TableCell}"><TextBlock Text="{Binding SizeText}" Style="{StaticResource TableText}"/></Border>
<Border Grid.Column="2" Style="{StaticResource TableCell}"><TextBlock Text="{Binding TableStatusText}" Style="{StaticResource TableText}"/></Border>
<Border Grid.Column="3" Style="{StaticResource TableCell}"><TextBlock Text="{Binding EtaText}" Style="{StaticResource TableText}"/></Border>
<Border Grid.Column="4" Style="{StaticResource TableCell}"><TextBlock Text="{Binding SpeedText}" Style="{StaticResource TableText}"/></Border>
<Border Grid.Column="5" Style="{StaticResource TableCell}"><TextBlock Text="{Binding DownloadedDateText}" Style="{StaticResource TableText}"/></Border>
                                        </Grid>
                                    </DataTemplate></ListView.ItemTemplate>
                                </ListView>
                            </Grid>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
        <Grid Grid.Row="4" Padding="12,0" ColumnSpacing="20" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,1,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock Text="{Binding StatusText}" FontSize="12" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="1" Text="{Binding ActiveDownloadsSummary}" FontSize="12" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="2" Text="{Binding BandwidthSummary}" FontSize="12" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
